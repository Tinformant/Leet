```java
    public int climbStairs(int n) {
        int current = 1;
        int next = 1;
        while (n-- > 0) {
            int temp = next;
            next = next + current;
            current = temp;
        }
        return current;
    }
```
